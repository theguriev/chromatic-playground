import { useState, useCallback } from 'react'
import { Meta, Story, Canvas } from '@storybook/addon-docs'
import { Button } from '.'

<Meta title="Components/Button" component={Button} />

<Canvas>
    <Story name="Default">
        {options => (
            <Button {...options}>Ok</Button>
        )
        }
    </Story>
</Canvas>
